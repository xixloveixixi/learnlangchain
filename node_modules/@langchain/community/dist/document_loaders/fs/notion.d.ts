import { DirectoryLoader } from "@langchain/classic/document_loaders/fs/directory";

//#region src/document_loaders/fs/notion.d.ts
declare namespace notion_d_exports {
  export { NotionLoader };
}
/**
 * A class that extends the DirectoryLoader class. It represents a
 * document loader that loads documents from a directory in the Notion
 * format. It uses the TextLoader for loading '.md' files and ignores
 * unknown file types.
 */
declare class NotionLoader extends DirectoryLoader {
  constructor(directoryPath: string);
}
//#endregion
export { NotionLoader, notion_d_exports };
//# sourceMappingURL=notion.d.ts.map